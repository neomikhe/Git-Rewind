// Package timeline turns raw reflog and fsck data into a typed, ordered list of
// events — what happened, when, and a risk level (green/yellow/red) — that the
// TUI renders as a human-readable history of the repository.
package timeline
